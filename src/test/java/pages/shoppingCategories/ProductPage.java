package pages.shoppingCategories;

import org.openqa.selenium.By;
import pages.BasePage;

public class ProductPage extends BasePage {

    protected ProductPage(By locator, String name) {
        super(locator, name);
    }
}
